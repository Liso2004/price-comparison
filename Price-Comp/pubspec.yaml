name: testshop
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # ADDED — for SVG category icons
  flutter_svg: ^2.0.9

  # ADDED — for category images (PNG/JPG) with caching
  cached_network_image: ^3.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # ADDED — assets folder for category icons/images
  assets:
    - assets/images/
